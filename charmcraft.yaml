# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.

name: ubuntu-motd-server

type: charm

bases:
  - build-on:
    - name: ubuntu
      channel: "22.04"
    run-on:
    - name: ubuntu
      channel: "22.04"

summary: Operates the Ubuntu MOTD server app.

# (Required)
description: |
  Operates the Ubuntu MOTD server app

extensions:
  - flask-framework

config:
  options:
    MOTDS:
      description: |
        The MOTDs to be returned by the server.
      default: ""
      type: string
