name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@feat/add_terraform_tests-ISD-5149
    secrets: inherit
    with:
      self-hosted-runner: true
      self-hosted-runner-label: "edge"
